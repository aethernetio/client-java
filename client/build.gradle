plugins {
    id 'java-library'
}

dependencies {
    def coreVersion = '0.0.68'
    api "io.aether:client-server-api:${coreVersion}"
    api "io.aether:client-server-reg-api:${coreVersion}"
    api "io.aether:core:${coreVersion}"
}
